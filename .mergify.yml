pull_request_rules:
  - name: automatic merge for Dependabot pull requests
    conditions:
      - author=dependabot-preview[bot]
      - status-success=ngstack.code-editor
    actions:
      merge:
        method: merge
